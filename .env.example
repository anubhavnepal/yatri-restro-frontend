# Public API base URL used by the typed frontend service layer.
# This must be prefixed with NEXT_PUBLIC_ because browser-readable values
# are only exposed by Next.js when they use the NEXT_PUBLIC_ prefix.
# Do not place secrets in this file.
NEXT_PUBLIC_API_URL=
